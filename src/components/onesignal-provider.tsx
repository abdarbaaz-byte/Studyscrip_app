
"use client";

import { type ReactNode } from 'react';

// OneSignal has been removed as per user request.

export function OneSignalProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
